# 金蝶 WebAPI SDK - 快速开始

## 3步开始使用

### 第1步：配置账号信息

编辑 `kingdee_sdk/config.py`：

```python
KINGDEE_CONFIG = {
    "server_url": "http://your-server-ip/K3Cloud",  # 你的服务器地址
    "acct_id": "your_acct_id",                   # 数据中心ID（从URL获取）
    "username": "your_username",                            # 你的用户名
    "password": "your_password",                          # 你的密码
    "lcid": 2052                                   # 语言代码（2052=中文）
}
```

### 第2步：运行测试

```bash
python demo.py
```

### 第3步：在自己的代码中使用

```python
from kingdee_sdk import KingdeeClient, AuthType

client = KingdeeClient(
    server_url="http://your-server-ip/K3Cloud",
    acct_id="your_acct_id",
    username="your_username",
    password="your_password",
    auth_type=AuthType.PASSWORD
)

client.login()

# 查询物料
result = client.execute_bill_query(
    form_id="BD_MATERIAL",
    field_keys="FMaterialID,FNumber,FName",
    limit=10
)

client.logout()
```

## 文件说明

| 文件 | 说明 |
|-----|------|
| `demo.py` | 功能演示脚本 |
| `使用指南.md` | 详细使用文档 |
| `test_login.py` | 登录测试脚本 |
| `kingdee_sdk/config.py` | 配置文件 |

## 常用表单ID

- `BD_MATERIAL` - 物料
- `BD_CUSTOMER` - 客户
- `BD_SUPPLIER` - 供应商
- `SAL_SaleOrder` - 销售订单
- `PUR_PurchaseOrder` - 采购订单
